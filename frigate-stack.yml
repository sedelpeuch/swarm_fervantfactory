version: "3.8"

services:
  frigate:
    image: ghcr.io/blakeblackshear/frigate:stable
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - frigate_config:/config
      - ./frigate-config.yaml:/config/config.yaml
      - frigate_storage:/media/frigate
      - type: tmpfs
        target: /tmp/cache
        tmpfs:
          size: 100000000
      - type: tmpfs
        target: /dev/shm
        tmpfs:
          size: 1000000000
    ports:
      - "8554:8554"
      - "8555:8555/tcp"
      - "8555:8555/udp"
      - "5000:5000"
    environment:
      FRIGATE_RTSP_PASSWORD: "admin"
      CONFIG_HASH: "688cfc2c3a9416679b473b5961745e592217b8c9dff065c97e5db2c7901321a4"
    networks:
      - proxy
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.frigate.rule=Host(`frigate.fervantfactory.delpeuch.net`)"
        - "traefik.http.routers.frigate.entrypoints=web"
        - "traefik.http.services.frigate.loadbalancer.server.port=5000"

volumes:
  frigate_config:
    driver: local
  frigate_storage:
    driver: local

networks:
  proxy:
    external: true
